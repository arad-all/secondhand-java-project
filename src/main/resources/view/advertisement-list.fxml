<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.ComboBox?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.ListView?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.FlowPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<BorderPane xmlns="http://javafx.com/javafx" xmlns:fx="http://javafx.com/fxml"
            fx:controller="frontend.controller.AdvertisementListController">
    <top>
        <VBox spacing="10">
            <padding>
                <Insets bottom="10.0" left="15.0" right="15.0" top="15.0"/>
            </padding>

            <HBox alignment="CENTER_LEFT" spacing="10">
                <Label style="-fx-font-size: 22px; -fx-font-weight: bold;" text="Advertisements"/>
                <HBox fx:id="navBox" alignment="CENTER_RIGHT" spacing="10" HBox.hgrow="ALWAYS">
                    <Button onAction="#handleGoToMyAdvertisements" text="My Advertisements"/>
                    <Button onAction="#handleGoToFavorites" text="Favorites"/>
                    <Button onAction="#handleGoToPurchaseHistory" text="Purchase History"/>
                    <Button fx:id="adminPanelButton" onAction="#handleGoToAdminPanel" text="Admin Panel"/>
                    <Button onAction="#handleCreateNew" text="New Advertisement"/>
                    <Button onAction="#handleLogout" text="Logout"/>
                </HBox>
            </HBox>

            <FlowPane hgap="10" vgap="8">
                <TextField fx:id="keywordField" prefWidth="220" promptText="Keyword"/>
                <ComboBox fx:id="categoryComboBox" prefWidth="180" promptText="Category (any)"/>
                <ComboBox fx:id="cityComboBox" prefWidth="150" promptText="City (any)"/>
                <TextField fx:id="minPriceField" prefWidth="100" promptText="Min price"/>
                <TextField fx:id="maxPriceField" prefWidth="100" promptText="Max price"/>
                <ComboBox fx:id="sortComboBox" prefWidth="170" promptText="Sort by"/>
                <Button onAction="#handleSearch" text="Search"/>
                <Button onAction="#handleClearFilters" text="Clear Filters"/>
                <Button onAction="#handleRefresh" text="Refresh"/>
            </FlowPane>
        </VBox>
    </top>
    <center>
        <ListView fx:id="advertisementListView" BorderPane.alignment="CENTER"/>
    </center>
    <bottom>
        <Label fx:id="errorLabel" style="-fx-text-fill: red;" wrapText="true">
            <BorderPane.margin>
                <Insets bottom="10.0" left="15.0" right="15.0" top="5.0"/>
            </BorderPane.margin>
        </Label>
    </bottom>
</BorderPane>
